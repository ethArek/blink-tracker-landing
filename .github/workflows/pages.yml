name: Deploy to GitHub Pages

on:
  # Triggers on push to master branch (including PR merges)
  push:
    branches: ["master"]
  # Allows manual deployment from Actions tab
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Stage static site
        run: |
          mkdir -p .site
          cp index.html stories.html styles.css main.js favicon.svg .site/
          cp -R downloads .site/
          touch .site/.nojekyll
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ".site"
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
